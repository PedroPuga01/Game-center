import React from 'react'
import styles from './main.module.css'

const Main = () => {
  return (
    <main className={styles.mainContainer}>
        <section>
          <h2>Bienvenidos!</h2>
        </section>
      </main>
  );
};

export default Main;